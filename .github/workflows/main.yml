name: Sistema Integrado Jarvis 2026

on:
  workflow_dispatch: 
  schedule:
    # Roda a cada 6 horas para coletar dados e atualizar o site
    - cron: '0 */6 * * *'
    # Roda uma vez por dia às 23:30 para fechar a assertividade do dia
    - cron: '30 23 * * *'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      
    steps:
      - name: 📥 Baixando Repositorio
        uses: actions/checkout@v3

      - name: 🐍 Configurando Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: 📦 Instalando Dependencias
        run: pip install pandas requests

      - name: 🚀 Coletar Novos Jogos (Manhã/Tarde)
        run: python coletor_jarvis.py

      - name: 🏆 Fechamento de Assertividade (Noite)
        run: python fechamento_jarvis.py

      - name: 📊 Atualizar Big Data Semanal
        run: python baixar_big_data.py

      - name: 💾 Salvando Evolução no GitHub
        run: |
          git config --global user.name "Jarvis Intelligence"
          git config --global user.email "bot@jarvis.ia"
          git add data/*.csv
          if git diff --cached --exit-code; then
            echo "Sem alterações para salvar."
          else
            git commit -m "📊 Protocolo Jarvis: Coleta e Assertividade Atualizadas"
            git push
          fi
